import { IconType } from "react-icons";

export type MainFooterDataType = {
  id: number;
  text: string;
  href?: string;
  icon?: IconType;
};

export const mainFooterData: MainFooterDataType[] = [
    
];
